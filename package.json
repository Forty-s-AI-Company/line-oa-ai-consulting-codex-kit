{
  "name": "line-oa-ai-consulting-codex-kit",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm -C apps/api dev",
    "db:generate": "node scripts/with-default-env.mjs pnpm -C packages/db prisma generate",
    "db:push": "node scripts/with-default-env.mjs pnpm -C packages/db prisma db push",
    "db:seed": "node scripts/with-default-env.mjs pnpm -C packages/db prisma db seed",
    "db:studio": "node scripts/with-default-env.mjs pnpm -C packages/db prisma studio",
    "db:local:init": "pnpm db:push && pnpm db:seed",
    "db:postgres:generate": "pnpm -C packages/db prisma generate --schema prisma/schema.postgres.prisma",
    "db:postgres:push": "pnpm -C packages/db prisma db push --schema prisma/schema.postgres.prisma",
    "db:postgres:seed": "pnpm -C packages/db prisma db seed --schema prisma/schema.postgres.prisma",
    "line:rich-menu:b2c": "node scripts/line-rich-menu-b2c.mjs",
    "lint": "pnpm -r --workspace-concurrency=1 lint",
    "pretest": "pnpm db:push",
    "test": "pnpm -r --workspace-concurrency=1 test"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^2.0.0"
  }
}
